# 2026-09-11

## 简历优化咨询（三项目 + datakit）
- 用户提供了一版简历（只写了 project1 + 一句话 datakit），请我优化「技术含金量 / 企业标准」。
- 我的诊断（复用价值高）：
  1. 只写功能不写难题与取舍；2. datakit（最有工程价值）被压缩成一句；
  3. 数据取证的坑（BRNUM 52.19% / 坐标精度词表切换 / 缺失非随机）全漏；
  4. 方法论上的「主动放弃」（SAR ρ 越界 → 降 SLX）没写；
  5. project6（31.1 GB / 28/28 断言 / 交互教材）完全没进简历；
  6. 项目名 `UCP-and-IS` 读不懂。
- 产出改写稿：拆成 3 条（① datakit SDK 工程线 ② 空间因果数据产品 ③ 生存 + 教学）。
  已在回复中给出完整文案，用户尚未确认是否落成文件。

## 简历可用弹药（已核对产物，可直接引用）
- datakit：2,861 行 / 13 模块 / 40 单测 / 8 种 Role / 6 类格式 / 3 项目 26 阶段实例 / 32.7 GB /
  replication_manifest（版本+参数+种子+输入指纹）/ 扩展阶段 blocking:false 降级 / 源数据只读。
- project1：282.3 万行面板、152,538 网点、27,018 关闭事件、H3 R8 70,109 格；
  BRNUM 首末年变化 52.19%（并购子样本 86.80%）→ 改 UNINUMBR；
  坐标精度 1994–2022 屋顶级 16.37% vs 2023–2025 EXACT 85.98%；
  4.26% 缺失（120,261 行）100% 集中 1994–2010；坐标缺失 8.04%；
  cKDTree 粗筛 + 向量化 haversine 精筛，330,691 对；处理组 35,785 / alive 76,097；同城对照覆盖 98.3%；
  PanelOLS TWFE（entity+time FE，cluster by entity）post=-0.05259；事件研究 τ0=-0.0438 → τ4=-10.1pp；
  SLX W_treat=0.008986 (p≈0.0013)；残差 Moran I=0.0703 (p=0.001)；SAR ρ=-0.170（不稳，已降级）；
  端到端约 26 分钟。
- project2：面板 1,667,283（事件率 4.13%），剔左截断 11,787；
  logit + cloglog（206,724 行 / 68,908 事件 / 伪 R² 0.387）；AUC 0.8814 / C-index 0.8096 / Brier 0.1609；
  残差 Moran I=0.1412 (p=0.005, k=8, 199 置换)；SHAP：year > 银行层脆弱性 > 网点规模。
- project6：4 数据集 / 24,975 文件 / 31.1 GB / 19,915,182 行；04 断言 28/28；05 合计 604,173 格；
  Moran's I：fdic 0.1331 / sz_bike 0.7077 / brightkite 0.0132 / gowalla 0.5090；
  换权重 fdic Queen 0.1331 / KNN k=4 0.1387 / KNN k=8 0.1181；brightkite log1p 后 0.013→0.285。

## ⚠️ 待用户处理的产物不一致
- SLX W_treat：简历/estimate.json = 0.008986 (p≈0.0013)，
  但 `project1/08_conclude/output/conclusion.md` 写 0.0073 (p=0.009)。口径需对齐（建议以 estimate.json 为准）。

## 用户偏好补充
- 关心简历「技术含金量 / 企业标准」，倾向把项目拆条、要取舍与失败披露，而非功能罗列。
- **产品观（重要）**：要求「先符合是一个产品（业务/需求/产品/市场），再是一个项目，
  最后才是开发者视角」；认为单个项目的展现形式不足以支撑整体，需要包装成产品/网页。
- 明确约束：**只在项目空间内新增，不修改既有文件、不改工作区外环境**。

## 用户否决 v1 门户：「你这不算是产品，只是汇报」（重要反馈）
- 用户明确指出：必须有**输入 → 计算 → 输出**才是产品；九个介绍页面不算。要求「先设计」。
- 我用 3 个 code-explorer agent（产品/业务/面试官视角）互相挑刺审查仓库，产出 `portal/DESIGN.md`（v2 设计稿）。
- **核心设计转向**：不要"把已有产物讲成产品"（会触发包装反噬——每条已知限制都变成"明知有问题还卖"），
  而要做**「这个结论能不能用」的诊断台**：把限制本身做成产品功能（强度评级/准入体检/适用性守门/泄漏自检）。
- v2 四模块：① 数据准入体检（首页，不受任何硬伤影响，最易成交）
  ② 冲击评估器（需先修 P0-1/2/4）③ 风险**归因**台（改名，不再叫预测；需先修 P0-3）
  ④ 口径实验室（唯一无硬伤，可立即上线）。
- 技术形态：默认纯静态离线；可选 `portal/serve.py`（仅标准库）解锁"上传样本 → 真调 datakit 体检"。
  不做用户系统/计费/持久化客户数据（合规优势）。
- 新增分层：`assets/engines.js` 计算层独立 + node 单测（唯一会算错的地方）。

## 已交付：portal/ v2 计算型工作台（用户第二次否决后重做）
- 用户第二次判定 v1 不合格：「你这不算是产品，只能是一个汇报」→ 要求**输入 → 计算 → 可交付输出**。
- 又追加约束：**源数据不在本机**（`data_raw/` 只剩 README）→ 一律不得依赖重跑管线。
- v2 交付（全部在 `portal/`，git status 仍只有 `?? portal/`）：
  - `assets/engines.js`（546 行）：准入体检 / 冲击评估 / 风险归因 / 口径实验室 /
    **结论强度评级器** / 报告生成。纯函数，**内部零硬编码模型系数**，全部取 `SDP_DATA`。
  - `tests/engines.test.js`：**26 项单测全绿**（`node --test`），覆盖数值正确性、
    护栏行为（一票否决 / 适用域拒绝 / 退化类别拒绝）、报告降级路径。
    过程中修掉一个真 bug：结果变量层级标注为"一票否决级"却没真的加否决项。
  - `serve.py`：本地服务（仅标准库 + datakit/pandas）。`POST /api/intake` 上传 CSV →
    **真实跑 datakit `scan/read/profile/validate`** → 自动预填问卷。实测 6/7 断言通过。
    合规：仅监听 127.0.0.1，`portal/.tmp/<uuid>/` 响应后立即删除。
    datakit 的 `Dataset` 取数属性是 **`.frame`**（不是 `.data`）；
    `validate` 支持的 check 只有 unique / null_rate_le / no_negative / range /
    allowed_values / no_duplicates / row_count_ge|le / column_count_eq / columns_eq —— **没有 `not_null`**。
  - `build_data.py` 扩展：OLS 完整方程、敏感性审计、TWIR 外推披露、模型卡、
    `parse_map_md`（16 个派生字段真实区间）、`shap_force`、34 条产物血缘。
  - `assets/app.js` 重写（9 页，工作台为首页）：四模块表单 + 实时重算 + 报告导出（打印/复制/下载 .md/.json）。
  - 冒烟：最小 DOM 桩逐页渲染 9 页 + 四条计算链路 paint() 全绿（0 泄漏）。
- 关键设计：**把限制做成产品功能**（外推区红色警告 + 观测支撑域标注 + 零交叉点 /
  泄漏特征保守版双值 / 2016+ 年份拒绝打分 / P0 修补看板）。

## 已交付：portal/ 产品门户（网点决策台 SDP，v1，已被 v2 取代）
- 新建 `portal/`，git status 仅有 `?? portal/`，**未改动任何既有文件**。
- 内容：index.html + assets/{style.css,charts.js,app.js} + data/{content.js,portal_data.js,.json}
  + build_data.py + README.md。9 个页面，顶部导航/侧边目录/全文搜索/深色模式/移动抽屉/打印样式。
- 技术决策：
  - 图表**纯手写 SVG**，不引 Plotly CDN → 保证 file:// 双击可开、零外部依赖；
  - 数据用 `<script>` 挂 `window.SDP_DATA`（非 fetch）→ 规避 file:// 的 CORS 限制；
  - 事实层自动生成、叙事层手写且不含数字 → 改文案不碰数字，重跑脚本不冲掉文案。
- 冒烟：node --check 全过；用最小 DOM 桩逐页渲染 9 页，6/6 SVG 生成，
  关键数字 −4.38pp / −10.07pp / p=0.0013 / AUC 0.8814 均命中；临时桩 `.smoke.js` 已删。
- 兼容性加固：搜索停用正则后顾（旧 Safari 会整体报错）；`color-mix` 补回退色。
- 遗留待办：
  1. `08_conclude/conclusion.md` 的 SLX W_treat=0.0073(p=0.009) 与 estimate.json 的
     0.008986(p≈0.0013) 不一致 → 门户以 estimate.json 为准，结论文档待修。
  2. 商业化文档里的市场/客单/人日等 `XX` 占位仍未填（刻意不臆测）。
  3. 需求尚未验证（无付费客户），已设计 90 天三个验证动作。

## 已交付：portal/ v2 代码评审 + 整改单（第三次介入，只读不改）
- 用户要求：审核另一会话（`2f47125bce50449cb24eb0d93fa9059b`）实现的 `portal/` 产品，给优化方案并可转交该会话。
- 产出新文件 `portal/REVIEW_20260911.md`（**未改动任何既有文件**，git status 仍只有 `?? portal/`）。
- **最重发现（引擎数值错误，已复算）**：`engines.js:183`
  `const b1 = tw.post_x_strength, b1se = tw.post_x_strength;`
  → 把交互项**系数**当**标准误**（数据层 `portal_data.json:421` 已有正确 `post_x_strength_se=0.001712`）。
  默认强度 1.61 处：正确 SE≈0.0032（CI≈[−3.66pp,−2.41pp]，显著）；
  现状 SE≈0.0223 → CI≈[−7.41pp,+1.34pp]，**跨 0**。同错值污染 `app.js:874` 强度曲线置信带。
  26 项单测**未覆盖**边际效应 SE，故逃逸。
- **P1**：同一项目两个结论等级（`computeGrade` vs `engines.js:261` 把 exogenous 写死 null）：
  外生 A 示例下工作台/报告=B、冲击页=C。
- **P1**：`tools.js:26/43` 示例写 `exogenous: true/false`（布尔），问卷 enum3 存 `'yes'/'no'`，
  三处转换（app.js:97/556/824）按字符串判定 → 示例载入后按钮不高亮、等级误降 B。
- **内容层自相矛盾**：`content.js:68` 仍叫「风险预测台」、`:86`「这是预测模型」、`:291`「前 τ 轻微为负」、
  `:12` version v1.0；`:73-79` metrics 与 `:37-58` valueProps 在 v2 app.js **从未渲染**（迁移未收尾）。
  `tools.js:2` 自称"不放任何数字"却有 ≥12 处硬编码模型统计。
- 另：报告血缘表模块列恒为 `—`；格级 OLS `+0.007313` 与 SLX 本地 `−0.005888` **符号相反**却未披露；
  `data-print` 从未加 `body.print-report`（style.css:320 是死代码）。
- 结论口径：**方向对、工程底子真实，但当前"能演示不能交付"**；整改单含验收清单与"最小上下文包"（只需 6 文件，无需重跑管线）。

## portal/ 文件盘点 + 清理 + 跨会话记忆整合（2026-09-11）
- 用户要求：① 查看 `portal/` 内文件并**给出文件大小**；② 清理不必要信息；③ 把会话
  `fb339334130f408687b2eeec4be12518` 与 `2f47125bce50449cb24eb0d93fa9059b` 的记忆**整合到本空间**。
- 盘点：清理前 16 文件 / 404,773 B（≈395 KB）；清理后 **15 文件 / 403,622 B**。
  最大的三个：`assets/app.js` 76,943 / `data/portal_data.js` 55,994 / `data/portal_data.json` 55,879。
- **清理（仅删运行期产物，未动任何交付物）**：
  - `portal/.serve.log`（1,151 B，`serve.py` 本地服务的请求日志，非页面引用）→ 删除；
  - `portal/.tmp/`（空目录，`serve.py:340-341` 会 `mkdir(exist_ok=True)` 自动重建）→ 删除。
  - 判定依据：`index.html` 只加载 `data/portal_data.js`，但 **`portal_data.json` 不删**
    —— `build_data.py:12-13` 明确它是对外程序可读的同内容副本，属刻意设计产物。
- **会话记忆定位**（重要工程经验）：CodeBuddy 会话历史在
  `%LOCALAPPDATA%/CodeBuddyExtension/Data/<uid>/CodeBuddyIDE/<uid>/history/<workspace-hash>/<session_id>/`
  （本工作区 hash = `f27c45eca0c0b53c8807b6d0e8f2976d`），结构为 `index.json` + `messages/<mid>.json`。
  **完整路径 > 260 字符，Python `os.path.exists` 会返回 False**，必须加 `\\?\` 前缀才能读写。
- **整合产出**：新建 `.codebuddy/memory/CONVERSATIONS.md`，收录两个会话的诉求演化链与拍板结论
  （详见该文件）；`MEMORY.md` 同步修正 portal 的过时描述（原写 v1 九页门户，实为 v2 工作台）。
