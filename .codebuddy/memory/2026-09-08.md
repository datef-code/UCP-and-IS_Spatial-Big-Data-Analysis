# 2026-09-08

## datakit/PROJECT_STRUCTURE.md 规范补强（仅改此文件，未动项目内容）

用户反馈规范文档存在 5 处缺失，本次只修改 `datakit/PROJECT_STRUCTURE.md`：

1. `02_profile` 补「角色/等级判定依据」：新增 §3.5「字段角色与等级：语义 + 判定依据」，
   含 role/level 语义表、按顺序的等级判定依据、主键判据（唯一/非空/稳定/业务标识），
   并用 FDIC `BRNUM vs UNINUMBR` 作「唯一≠稳定」反例；§2-② 加指针说明。
2. 补数据集说明与立项：新增 §9「数据、立项与 README 的说明规范」——项目主 README 是
   立项文件（立项六问）、data_raw/README 可信度评估、各阶段/插件 README 作用定位。
3. `03_clean` 补决策理由框架（drop_na/drop_columns/fill/to_null/保留缺失 的「为什么」），
   明令禁止对空间列/主键/事件时间编造式填充或截断。
4. `05_map` 补三件事：坐标缺失→H3 置空的可信度量化、映射如何支撑研究、血缘为何需要。
5. 目录树 README 注释同步更新；「待确认」由 §9 顺延为 §10。

项目侧实际内容（project1_fdic_spatial 各阶段 README 等）按要求暂未改动。

## 三产品「展现形式」升级建议（已落盘）

用户认为静态 PNG/PDF 图不足以支撑产品展示与信服度，要求研究更好的表现形式并**按产品差异化划分**。

最终产出 `datakit/suggestions_for_projects_0908.md`，核心结论：
- 产品1（空间溢出）= 证据链叙事：王牌是时空演变动画 + Plotly 事件研究/距离衰减交互 + Kepler 时间轴。
- 产品2（生存模型）= 可上手产品 demo：预测 demo + SHAP 交互 + 模型体检单页，必须标注「相关≠因果」。
- 产品6（教学）= 可复现交互教材：Jupyter Book/Quarto 电子书 + 可交互 Moran 散点 + Binder 一键复现。
- 落地优先级：先 Plotly 化 + 一键复现（共用低成本），再做各自王牌，最后以 L0→L3 阶梯作统一门户。
