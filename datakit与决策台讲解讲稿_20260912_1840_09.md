# datakit、产品6 与决策台：从数据底座到可交付决策产品

> 讲解讲稿（含现场演示动作提示）
> 对应材料：`html/datakit作用与产品6下一步方法_20260912_1807_08.html`、`portal/`（网点决策台 SDP）
> 时长参考：约 25–30 分钟

---

## 开场

各位好。今天我想用一条线，把三件事串起来讲清楚：**datakit 是底座，产品6 是方法，决策台是把前两者整合成的、一个真正能拿去开会、能被质询时站得住的产品。**

为什么要这样串？因为单独看任何一个都容易看偏：datakit 单独看是一堆工程模块，产品6 单独看是一套教学阶梯，而决策台单独看会让人以为它只是又一个"数据看板"。只有把它们放在一条链上，才能看出价值是怎么一层层长出来的——

**datakit 保证"算得可复现"，产品6 保证"方法没算错"，决策台保证"结论敢不敢用"。**

我先讲前两层，再重点带你上手第三层。

---

## 第一部分：datakit —— 先把地基打好

### 它是什么

一句话：datakit 是一个**通用数据处理包**。给它一个数据目录或一个数据文件，它能像 pandas 处理表格、numpy 做数值计算那样，用一组方法把整条链路做下来。这条链路是五段：

**采集 → 描述性统计 → 清洗 → 校验 → 映射。**

展开讲就是：

1. **采集**：扫描目录，列出数据集清单、体积、指纹与分组；
2. **描述性统计**：逐字段画像，给出类型、缺失率、异常值和不符合业务规则的值；
3. **清洗**：按配置执行删行、填充、截断、置空、类型转换，并且**每一步都记录"动了什么、为什么、影响多少行"**；
4. **校验**：用断言检查清洗结果，给出清洗前后的逐字段对比；
5. **映射**：把原始字段映射成分析字段，记录每个输出字段的来源与表达式。

### 为什么要有它

这里我想强调一个很多人会忽略的点：datakit 的价值不在"能跑通一次"，而在**别人接手、三个月后重跑、被审计时都还说得清**。

它靠三个机制做到这一点：

- **每一步留痕**：清洗的每一个动作都写进决策日志，不是事后回忆"我当时为什么删了这几行"。
- **双份报告**：每个阶段同时输出机读报告（JSON/YAML）和人读报告（Markdown），机器能对、人能看。
- **源数据只读**：它从不写回源文件，产出只写指定输出目录。

所以 datakit 的本质是：**用工程手段，把"靠自觉"变成"靠契约"。** 数据项目最大的成本不是跑通，而是自证——这个底座解决的就是自证。

### 它的组件

五个阶段部件负责数据本身，另外一组支撑部件负责让整条链路跑得起来、查得到：

- 数据容器 `core`、多格式读写 `io`（CSV/TSV、JSON/JSONL、XML、RDF、XLSX、gz 压缩文本）、字段规格 `schema`、报告写出 `report`、流程编排 `pipeline`、项目脚手架 `project`，还有一份结构规范文档。

（如果听众是技术背景，可以补一句：目前是 13 个模块、2,861 行、40 个用例，都是实测计数，不是约数。）

好，底座讲完。它只回答"数据干净不干净、能不能追溯"，但**它回答不了业务问题**。业务问题要往下走。

---

## 第二部分：产品6 —— 空间方法的四阶阶梯

### 它接在 datakit 之后

产品6 拿的是 datakit 第五步（映射）产出的那张分析表，把表里的**坐标两列**继续用下去，变成一套**可计算、可验证的空间结构**。

为什么要专门做这一步？因为有一类问题，**表本身答不了**：

- 谁是邻居？
- 值聚不聚集？
- 影响能传多远？

这些都不在表格的某一列里，必须先把"空间结构"建出来才能算。产品6 就是把这件事固化成四个递进的台阶。

### 四阶：L0 → L1 → L2 → L3

**L0 · 网格化**：把散点聚合到六边形网格，逐格汇总格内所有点。得到"格 + 格值"。它能回答：这一格有多少？格值分布长什么样？

**L1 · 空间权重矩阵**：定义格与格的邻接关系，并做行标准化。得到邻接关系 + 平均邻居数 + **孤岛格**（没有邻居的格）。它能回答：谁是邻居、有多少邻居、有多少格是孤岛？

这里我要敲一下黑板：**L1 这一步的选择，直接决定后面 L2 的结论。** 邻居怎么定义，是个"人为选择"，不是客观事实。

**L2 · 空间滞后与自相关**：把邻居的值加权平均到自己身上（这叫空间滞后），再算全局自相关（Moran's I）。它能回答：相似的值是聚在一起，还是散开的？聚得有多强？

**L3 · 距离环溢出**：取热点格为圆心，按距离划出若干环，统计各环内的溢出量。它能回答：影响传多远、衰减多快？

### 这里有一个非常重要的陷阱

L3 这一步，同时给"求和"和"密度"两种口径，原因是：**只看"求和"会得出相反的结论**——环越大、包含的格越多，和自然越大，但这不代表影响越强。必须看密度、看衰减斜率，才能说"影响在 3 公里内就衰减完了"。

### 产品6 的核心主张和边界

核心主张一句话：**「邻居」怎么定义，决定你能看到什么结论。** 同一份数据，换一种邻接定义或变量尺度，算出来的聚集指标会明显改变。

边界也要说清，这套方法：

- **不给因果结论**：它描述"聚集/衰减"，不回答"谁导致谁"；
- **不替人定口径**：网格粗细、邻接定义、距离环都由人选定，换设定结论就变；
- **孤岛格不参与滞后**：稀疏数据里孤岛占比可能很高，解读时必须一并说明。

方法讲完。**但是**——方法再好，如果它只是几段代码和几张图，它仍然不是产品。真正的产品，是把这些方法变成"输入参数 → 真实计算 → 可交付输出"的东西。这就是第三部分。

---

## 第三部分：决策台 —— 把方法变成一个能拿去开会的产品

### 它是什么

决策台（SDP，网点决策台）把四个东西整合成了一个**可操作的产品**：

- `project1`（空间溢出）——回答"关一家店，周边会怎样"；
- `project2`（寿命风险）——回答"哪些店最可能出事"；
- `project6`（空间口径）——就是刚讲的 L0→L3；
- `datakit`（工程底座）——保证每一步可复现。

它的一句话定位是：

> **把"关一家店，周边会发生什么"，从拍脑袋，变成一个能拿去开会、被质询时站得住的答案。**

### 它和普通看板的本质区别

这是我最想讲清楚的一点。普通 BI 看板卖的是"结论数字"——"这家店去年掉了 8%"。但决策台卖的**不是结论，而是"判断这个结论能不能用"的方法**。

具体说，决策台的每一个输出，都强制携带三件套：

1. **点估计**——这个数字是多少；
2. **不确定性**——它的置信区间、它的精度折扣；
3. **使用边界**——它**能用于**什么、**不能用于**什么。

最有代表性的是"结论强度分级"，它把结论自动判成三档：

- **A 级 · 准因果**：外生冲击 + 平行趋势通过，可对外披露；
- **B 级 · 关联**：可作内部决策参考，**不得对外宣称因果**；
- **C 级 · 描述性**：只能描述差异与情景分析，不得用于决策阈值。

这套分级不是手选的下拉框，而是由引擎按真实产物、按可执行阈值**自动判定**的，并且整个产品只有这一个评级器，工作台、报告、冲击页三处同源，绝不会出现"同一个项目在三个页面三个等级"的自相矛盾。

### 最反直觉、也最关键的设计：把限制做成功能

一般产品会把"已知缺陷"写进免责声明。决策台反着来——**把限制变成会自动触发的产品行为**。举几个例子：

- 模型系数是"暴露强度=0 处的外推"，而 0 这个点从未被观测过 → 结果页**强制标注观测支撑域下界**，输入落到外推区就弹红色告警；
- 风险模型存在一个目标泄漏特征 → 每次结果**同时给全特征值 + 剔除泄漏后的保守值**，并说明两者必须一起看；
- 年份超出模型适用域 → **直接拒绝打分**并解释原因，这是护栏，不是 bug；
- 有六条已知硬伤 → 全部挂在一个**公开的修补看板**上，看板说"待修"、实际已修，二者并列显示。

一句话总结：**能说清边界的供应商，才敢要价。** 这本身就是差异化。

---

## 第四部分：现场演示（重点）

下面我们实际走一遍。我会分两条路径：先走"只看不接数据"的 5 分钟演示，再讲"接自己数据"的完整流程。

### 4.1 怎么打开

【演示动作：双击 `portal/index.html`】

默认**纯静态、离线可开**，无需安装、无任何 CDN 依赖，图表是手写 SVG。双击就能打开。

要接自己的数据，才需要起本地服务——双击 `portal/start.bat`，脚本自动找 Python、检查依赖、挑一个没被占用的端口、起服务并开浏览器。

### 4.2 产品只有三个入口

打开之后你会看到，产品已经收敛成"一个项目 → 一份报告"的极简结构，只剩三个入口：

1. **① 数据与项目**——导入数据 + 填写项目信息；
2. **② 数据准入体检**——门禁：能不能做、能做到什么等级；
3. **③ 评估报告**——主交付物。

注意：冲击评估、风险归因、口径检验**不再是独立页面**，而是评估报告的第 ①②③ 章，它们的参数滑杆收进报告底部的"参数实验室"。

### 4.3 路径 A：5 分钟看懂它（演示示例 A）

【演示动作：打开"数据与项目"页 → 滚动到底部 → 点"载入示例 A"】

先载入内置的**示例 A · 咨询机构的连锁零售客户**：一个便利店连锁，有 10 年门店级销售数据、坐标完整、事件是竞对破产清算（外生）。决策问题是："竞对破产清算后，我方 3 km 内门店的销售额影响有多大、传多远？"

载入后，顶部会出现"建议下一步"卡，跟着它点即可。

**第①步 准入体检**。产品问 7 个问题，并当场判定。这个示例的结果是：

- 稳定实体 ID：有；
- 匹配率 94%、坐标缺失 3%、门店级指标、时间跨度 10 年、事件可观测、**外生冲击**。

于是它给出 **Go、可行性得分 100%、结论强度上限 B 级、预计工期 8–15 周**。

【演示动作：点进"冲击评估"章】

**第②章 冲击评估**。拖"暴露强度"滑杆（这个产品的定义是 `log1p(5 km 内同业关闭事件数)`）。你会看到三样东西：

- 一条强度—效应曲线，带 95% 区间，**绿色阴影是观测支撑域**；
- 逐年事件研究曲线，看影响第几年最深；
- **本地 / 邻域分解**——本地被吸走、邻域补回，方向相反，说明是"存款再配置"而不是区域净增。

这里要特别强调一个数字的引用规范：产品强制展示"**均值处边际效应 −4.31pp，95% CI [−4.71, −3.92]**"，而不是那个"强度=0 处的外推值"。因为 0 这个点从未被真实观测过，不能单独引用。

【演示动作：点进"风险归因"章】

**第③章 风险归因**（注意：它叫"归因"，不叫"预测"）。点三个预设样本（低/中/高风险），看因子贡献表里变化最大的是哪一项——在这个项目上答案是"所属银行历史关闭率"，也就是"谁家的网点"比"网点多老"重要得多。但它同时是泄漏特征，所以产品并列给出"剔除泄漏后的保守值"。

【演示动作：点进"口径检验"章】

**第④章 口径检验**。切数据集、切权重、切尺度，看 Moran's I 从 0.0132 一路到 0.7077——这就是产品6 的 L0→L3 直接变成了一个能拨的开关。这里顺便有个合规点：SNAP 两个数据集禁商用，商用版只剩 fdic 和 sz_bike。

【演示动作：进"评估报告"页】

**第③步 评估报告**。进入即自动生成，包含门禁横幅、KPI 条、结论强度、证据/机制/风险/口径/限制六章，最后带一个**非加密指纹**——同一组输入 + 同一版数据层，就是同一个指纹，用来回答"手上这份 PDF 对应哪一版数据"。

### 4.4 演示示例 B：产品会劝你别硬做

【演示动作：回"数据与项目"页 → 载入"示例 B"]

这是另一个内置示例：**某银行并购整合**。它的数据条件差很多——只有公司级汇总指标、坐标缺失 28%、事件是自主决策。

结果如何？**触发一票否决**，可行性得分只有 38%，结论强度上限直接判不出来。两条阻断项写得很清楚：

1. 坐标缺失率 > 20% → 空间口径不可用；
2. 结果变量非门店级 → 无法构造门店级 outcome，DID 与事件研究都不成立。

这就是决策台想教的东西：**示例 A 演示"一个能做的项目长什么样"，示例 B 演示"产品会劝你先去补数，而不是把不合格的数据硬做成一份好看的报告"。** 这是它区别于所有"只要给钱就给报告"的产品的地方。

### 4.5 路径 B：接自己的数据

【演示动作：起本地服务 → 进"数据与项目" → 数据接入】

接自己数据时，有三件事值得讲：

1. **多份文件、字段还不一样也能接**：按列名并集纵向拼接，缺列留空并登记；字段角色靠**取值分布自动推断**（年份靠值域、坐标靠值域+成对、实体靠"期上重复"），不靠列名，推断结果可人工覆盖并存成模板。

2. **能力判定是数据条件的函数，不是开关**：单一年份 → 没有时间维，算不了 DID，模块就显式显示 ✗ 并说明原因；有实体+期且唯一 → panel，才给你 ✓。绝不硬套一套不适用的系数。

3. **数据不出本机**：服务只监听 127.0.0.1，上传的文件在响应结束后立即整目录删除，不持久化客户数据。大数据建议用"本机路径"模式，连复制都不会发生。

上传后可选"现场重估"——用客户自己的数据现场拟合 TWFE/事件研究/空间/cloglog，系数替换进产品，四个模块立刻按新系数工作，顶部黄条提示"当前数据层来自现场重估"，一键可恢复仓库产物。

### 4.6 怎么交付

走完流程，到评估报告页：

- **打印 / 导出 PDF**：报告正文（打印时自动隐藏导航与表单），交给客户；
- **下载 .md**：Markdown 全文，给协作方二次编辑；
- **下载结果 JSON**：`{project, computed, dataLayer}`，交给下游程序；
- **导出项目 JSON**（在数据与项目页）：用于存档，下次导入即可恢复全部输入。

---

## 收尾

好，最后我把这条线收一下。

今天讲的三件事，其实是一个递进关系：

- **datakit** 保证的是——三个月后重跑、换人接手、被审计时，每一个数字都能追到具体产物；
- **产品6** 保证的是——空间方法没有算错，而且"邻居怎么定义决定你看到什么结论"这件事被明明白白摆上台面；
- **决策台** 保证的是——结论敢不敢用、能用什么等级，不再靠人拍脑袋，而是被写进产品、自动触发。

三者合起来，才把"空间决策"从一份分析报告，变成一个**输入参数 → 真实计算 → 可交付输出**的产品。

而贯穿始终的一句话是：**不要盲信一个数。** 能说清一个数字的边界，本身就是这套东西最值钱、也最可信的地方。

谢谢大家，欢迎提问。
