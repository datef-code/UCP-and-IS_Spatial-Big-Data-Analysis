# 三项目事实核查报告 · 2026-09-08

> 范围：`project1_fdic_spatial` / `project2_fdic_survival` / `project6_spatial_teaching`
> 依据：`datakit/PROJECT_STRUCTURE.md` v2（§2 报告块、§3.5 角色与等级判定依据、§8 扩展阶段、§9 三层 README）
> 方法：**联网核实官方来源** + **本地源数据全量实测**（两条独立证据链互证），结论只写能复算的数字。
> 核查脚本：一次性脚本，核查完成后已删除（见文末「复算方式」）。

---

## 0. 一句话结论

三个项目的**结构合规度很高**（八阶段骨架、`run(project)` 契约、config 口径外置、扩展阶段登记、
manifest / lineage / replication_manifest 全部到位，全项目 0 处 TODO 占位），
但**文档里有 6 处事实性错误 / 口径缺陷**，其中 3 处会影响结论解读；
另有 **1 处共享 SDK 记账缺陷**（越界置空被记成「截断」，与规范 §2-③ 的明令禁止直接冲突）。
以上均已修正，并补齐了规范 §3.5 / §9.2 / §9.3 要求的缺失内容。

---

## 1. 核实为真的结论（沿用，无需改）

| # | 项目 | 声明 | 核实方式与结果 |
| --- | --- | --- | --- |
| V1 | p1 / p2 | FDIC SOD 覆盖 1994–2025，32 个年度文件 | ✅ 本地实测 32 个 `fdic_sod_<YYYY>.csv`，1,681,231,439 字节（≈1.57 GiB） |
| V2 | p1 / p2 | 81 列、跨年列结构一致 | ✅ 逐年读表头比对，**32 年全部 81 列，无列漂移** |
| V3 | p1 / p2 | 追踪主键 `UNINUMBR` 而非 `BRNUM` | ✅ `UNINUMBR` 非空后唯一值 = 网点数；`BRNUM` 观测 ≥2 年网点中 **52.19%** 首末年变化 |
| V4 | p1 / p2 | 清洗删除 4.26% | ✅ 实测 `UNINUMBR` 缺失 120,261 / 2,822,977 = **4.2601%**，与 ③ 删除率完全一致 |
| V5 | p2 | 坐标缺失 8.04% | ✅ 实测 226,932 / 2,822,977 = **8.04%** |
| V6 | p2 | `DEPSUMBR` 无负值 | ✅ 全期负值 **0 条** |
| V7 | p6 | H3 R8 网格尺度 | ✅ h3 4.5.0 实测：平均单元面积 **0.737328 km²** |
| V8 | p6 | `snap_gowalla` 规模与时间 | ✅ 本地 6,442,892 行 vs 官方 6,442,890（+2，可忽略）；时间 2009-02-04 ~ 2010-10-23 与官方「Feb 2009 – Oct 2010」一致 |
| V9 | p6 | `sz_bike` 数据源已停更（2021-08） | ✅ 实测文件名日期范围 2020-01-01 ~ **2021-08-31** |

---

## 2. 核实为假 / 口径错误的结论（已修正）

### E1 · `CBSA_METRO` 的说法不成立（p1）

| 项 | 内容 |
| --- | --- |
| 原表述 | 「`CBSA_METRO` **早年** SOD 不存在，用 `MSABR` + `METROBR` 作代理口径」 |
| 实测 | **1994–2025 全部 32 个年度文件、81 列中均不含 `CBSA_METRO`**；只有 `MSABR` / `METROBR` / `MICROBR` / `CSABR` / `CBSA_DIV_NAMB` |
| 性质 | 「早年缺失 → 权宜替代」是错的。正确说法是：**SOD 全期都没有该字段**，都会区只能用 `MSABR` + `METROBR` |
| 已改 | `README.md`、`data_raw/README.md`、`config/schema.yaml`、`02_profile/02_profile.py` 注释 |

### E2 · 「EXACT 占 45.45%」不是坐标精度度量（p1，影响结论解读）

| 项 | 内容 |
| --- | --- |
| 原表述 | 「`SIMS_PROJECTION=EXACT` 占比 45.45% → 坐标存在插值」 |
| 实测 | 该字段的**取值词表在 1994–2025 间至少换过两轮**：<br>• 1994–2022：`US_Rooftop` / `US_Streets` / `US_Zipcode` / `0` / `100`<br>• **2023–2025 才出现 `EXACT`**（配 `StreetAddress` / `PointAddress` / `Postal`） |
| 性质 | 45.45% 是「各网点**最后观测年**的 projection 是否 = EXACT」的混合值。2023 年前退出的网点**天然不可能**为 EXACT → 该数字同时混淆了**编码体系切换**与**样本退出时间**，不能当精度指标 |
| 正确口径（分母 = 网点-年行） | 2023–2025：EXACT **85.98%**（198,260 / 230,594）<br>1994–2022：`US_Rooftop` **16.37%**、`US_Streets` 31.57%、`US_Zipcode` 4.97% |
| 结论影响 | 方向不变（早年精度确实差），但**量级远比原表述严重**：早年屋顶级只有 16%，而非「55% 非 EXACT」给人的温和印象 |
| 已改 | `README.md`、`data_raw/README.md`、`config/schema.yaml`、`config/assertions.yaml`、`04_validate/04_validate.py`（改为分年代 + `EXACT ∪ US_Rooftop`）、`06_estimate`、`08_conclude` 全链路 |

### E3 · H3 R8 尺度与 `grid_disk(k=23)` 半径写错（p1）

| 项 | 原文 | 实测（h3 4.5.0） |
| --- | --- | --- |
| R8 平均边长 | 「≈ 0.46 km」 | **0.531414 km**（0.46 不是 h3 4.5.0 的 R8 值）；平均单元面积 0.737328 km²；相邻单元中心平均距离 ≈ 0.89 km |
| `grid_disk(k=23)` | 「≈ 10 km 半径」 | 最大 **21.7 km**、平均 **13.0 km**（要覆盖 10 km 半径，k≈12，实测最大 11.4 km） |

> 重要澄清：`grid_disk(k=23)` 是**候选窗口**，真正的 10 km 由 haversine 过滤实现
> （候选窗口大于目标半径只会多算、不会漏算），**所以 10 km 环本身没有被放大**。
> 错的是 README 把它描述成「≈10 km 半径」。已改为「候选窗口 21.7 km + haversine 过滤到 10 km」。

### E4 · H3 分辨率配置与实现漂移（p2）

`config/mapping.yaml` 写 `h3.latlng_to_cell(lat, lng, 8)`，而实现 `05_map/05_map.py` 的
`H3_RES = 7`、`lineage.csv`、README、SUMMARY 全是 **R7**。
R7 实测平均单元面积 5.161293 km²、平均边长 1.406476 km——与降级网格 0.01°（≈1.1 km）同尺度，
**R8（0.737 km²）显然不匹配**。已把配置统一为 **res 7**（改配置而不是改实现，符合规范 §3「改口径改配置」），
并顺手修掉实现里两处写死的「≈1.22 km / ≈0.46 km」。

### E5 · SNAP 数据年代写错（p6）

| 项 | 原文 | 官方 + 实测 |
| --- | --- | --- |
| Brightkite | 「数据年代 **2010–2013**」 | 官方 **Apr 2008 – Oct 2010**；本地实测 **2008-03-21 ~ 2010-10-18** |
| Gowalla | 「数据年代 **2010–2013**」 | 官方 **Feb 2009 – Oct 2010**；本地实测 **2009-02-04 ~ 2010-10-23** |

两者都在 **2010 年**结束，「2013」无依据。已更正，并补上官方要求的两数据集引用
（Cho, Myers & Leskovec, *Friendship and Mobility*, ACM SIGKDD 2011）。

### E6 · `sz_bike` 源路径 glob 写错（p6）

原文 `sz_bike/bike_*_15_p*.csv` —— 实测本地**没有任何文件名含 `_15_`**，
真实命名是 `bike_<YYYYMMDD>_p<NNNNN>.csv`（24,939 个 CSV + 4 个采集元数据文件，**31.49 GB**，
覆盖 2020-01-01 ~ 2021-08-31 共 609 天）。抽样「每月 15 日」是按**日期**筛，不是按文件名。
已更正，并把「全量 30 GB」更新为实测 31.49 GB。

---

## 3. 共享 SDK 记账缺陷（已修，影响 p1 + p2）

`datakit/clean.py` 对 `on_violation: to_null` 的决策详情写的是中文「置空」，
而项目侧 `_impact()` 用 `"to_null" in detail` 判断是否置空 → **永远判否**，
于是「越界置空」被记进 `clipped`（截断）列。

这与规范 §2-③ 的明令禁止直接冲突：**截断会伪造一个看似合理的坐标/金额，
而置空是保留「未知」**——两者混记会让读者以为坐标被截断过。

| | 修正前 | 修正后 | 独立实测对照 |
| --- | --- | --- | --- |
| `filled_by_field.csv` | `clipped=5, clipped_to_null=0` | **`clipped_to_null=5, clipped=0`** | 全期坐标越界实测 = **5 条**（2024 年 3 条、2025 年 2 条）✅ |

---

## 4. 补全的规范缺口

### 4.1 `fields.csv` 缺「角色 / 等级判定依据」（规范 §2-②、§3.5）——p1、p2

原 `fields.csv` 只有枚举值，没有「为什么」。已：

1. 在 `config/schema.yaml` 为每个字段补 `note`（唯一性 / 非空性 / 稳定性 / 业务标识四条主键判据，
   以及「为什么不到 required」「为什么禁止 fill」的取舍理由）；
2. 改 `02_profile.py` 输出 `description` / `level_reason` / `n_issues` 三列，
   并在 `profile.md` 增开「字段角色 / 等级判定依据」整节；
3. **重跑 ② → ③ → ④**，产出已落地（例：`UNINUMBR` 的 `n_issues=3`，
   `level_reason` 写明「对照 BRNUM 52.19% 首末年变化 → 唯一≠稳定」）。

### 4.2 主 README 缺「立项六问」（规范 §9.2）——p1

p1 原有结构里没有立项背景 / 研究问题 / 创新点 / 空间大数据作用 / 可行性。
已补齐整节，其中「空间大数据起什么作用」明确回答了**去掉空间维度结论还成立吗**
（只剩同城粗对照，无法回答距离衰减、无法区分「本地被吸走」与「区域整体下行」）。

### 4.3 `data_raw/README.md` 缺「可信度评估」（规范 §9.3）——p1、p2、p6

三个项目都已补『覆盖范围 / 精度 / 缺失与已知偏差』小节，全部用本地全量实测数字。

### 4.4 新发现的「缺失非随机」事实（写进 p1、p2 的已知限制）

> **`UNINUMBR` 缺失 120,261 行（4.26%），100% 集中在 1994–2010；2011–2025 零缺失。**
> **`SIMS_LATITUDE` 缺失 226,932 行（8.04%），其中 97.8% 也落在 1994–2010。**

这是**时间结构化缺失**，不是随机缺失。对 p1 意味着事件研究的早期年份代表性弱；
对 p2 意味着「网点年龄 / duration」的分布对早年样本不具代表性、左截断处理须谨慎。
两条都已写入对应项目的 README 已知限制与 `08_conclude` 结论。

### 4.5 `fields.csv` 缺判定依据（规范 §3.5）——p6 同 p1/p2，已改

p6 只有 3 个公共字段（`lon` / `lat` / `value`），同样已补 `note`（判定依据）到 `config/schema.yaml`，
并改 `02_profile.py` 输出 `level_reason` / `n_issues`。**已重跑 ② 四个数据集全量**，
管线日志反过来又交叉验证了我的实测数字：

```
[sz_bike]         抽样日 20 天、873 个文件、8,572,465 次骑行
[snap_brightkite] 签到 4,747,287 条；时间范围 2008-03-21 … 2010-10-18
[snap_gowalla]    签到 6,442,892 条；时间范围 2009-02-04 … 2010-10-23
```

与独立实测完全一致（Brightkite 4,747,287 / Gowalla 6,442,892 / 年代 2008–2010、2009–2010）。

### 4.6 「数据说明书」根本不入库（三项目共性问题，已修）

规范 §9.1 / §9.3 把 `data_raw/README.md`（来源、获取方式、授权、更新频率、逐字段口径、
**可信度评估**）列为**必交付物**，并明确它是「立项可行性论证的事实来源」。

但仓库根 `.gitignore` 第 2 行是 `data_raw/` —— 三个项目的 `data_raw/README.md`
**全部未被 git 追踪**（`git ls-files project*/data_raw` 返回空）。后果：

- 任何人 `git clone` 后都拿不到数据说明书，§9.3 形同虚设；
- 本次补齐的「可信度评估」也只会留在本地。

已改为 **`data_raw/*` + `!data_raw/README.md`**：

| 路径 | 改前 | 改后 |
| --- | --- | --- |
| `project*/data_raw/README.md` | 被忽略 | ✅ **可追踪** |
| `data_raw/fdic/fdic_sod_2025.csv` | 被忽略 | 仍被忽略（数据不入库，符合原意） |

> 踩坑记录：必须写 `data_raw/*` 而不是 `data_raw/`。git 不会进入已被整体排除的目录，
> 写 `data_raw/` 时后面的 `!README.md` 取反**无效**——这一点已写进 `.gitignore` 注释。

### 4.7 p6 其它缺口

| 缺口 | 处置 |
| --- | --- |
| `logs/pipeline.log` 「缺失」 | 实为 `.gitignore` 第 11 行 `*.log` 导致**不入库**——全新 clone 后没有是预期行为。已在 `logs/README.md` 说明，并清掉过期的「迁移前写在 `output/pipeline.log`」描述（三项目同改） |
| `custom` 断言未实现时静默 `passed: True` | 改为**显式 failed + `implemented: false`**，杜绝「28/28 全绿但其实什么都没验」的假绿（规范 §3.4）。当前 p6 的 `weights_row_standardized` 在 `05_map.py` 里已用 `spatial_ladder.row_sum_ok()` 真实求值，不受影响；此改动是防御性加固 |
| `output_legacy_v1/` 无说明 | 补 `README.md`：旧 `01_import…06_conclusion` → 新 `01_discover…07_conclude` 映射表，以及「为什么不能直接比数字」（旧版不分数据集、无 config 外置、无 lineage/manifest） |
| `snap_brightkite` 行数与官方不符 | 本地 4,747,287 行 vs 官方 4,491,143 check-ins（**+256,144，+5.7%**）。实测**无空行、全部 5 字段**，但有 6 行时间戳异常、109 行坐标越界。差异不是解析错误，已在 `data_raw/README.md` 明确标注「引用计数时须说明用的是本地文件行数」 |

---

## 5. 复算方式（任何人可重跑验证）

核查用的一次性脚本已在核查完成后删除，下面给出等价的最小复算片段：

```python
import pandas as pd, h3
FD = r"E:\workspace\workbuddy\project\data_use\data_raw\fdic"

# V1/V2：年份与列数
import pathlib
cols = {y: list(pd.read_csv(f"{FD}/fdic_sod_{y}.csv", nrows=0).columns) for y in range(1994, 2026)}
assert len({len(c) for c in cols.values()}) == 1          # 81 列，无漂移
assert "CBSA_METRO" not in set().union(*cols.values())     # E1

# E2：分年代坐标精度
old = pd.concat([pd.read_csv(f"{FD}/fdic_sod_{y}.csv", usecols=["SIMS_PROJECTION"])
                 for y in range(1994, 2023)]).SIMS_PROJECTION.astype(str).str.upper()
new = pd.concat([pd.read_csv(f"{FD}/fdic_sod_{y}.csv", usecols=["SIMS_PROJECTION"])
                 for y in range(2023, 2026)]).SIMS_PROJECTION.astype(str).str.upper()
print((old == "US_ROOFTOP").mean(), (new == "EXACT").mean())   # 0.1637  0.8598

# E3：H3 R8 尺度
print(h3.average_hexagon_area(8, unit="km^2"), h3.average_hexagon_edge_length(8, unit="km"))

# V4/V5 + §4.4：缺失的时间结构
na = {y: int(pd.read_csv(f"{FD}/fdic_sod_{y}.csv", usecols=["UNINUMBR"]).UNINUMBR.isna().sum())
      for y in range(1994, 2026)}
print(sum(na.values()), sum(v for y, v in na.items() if y >= 2011))   # 120261  0
```

---

## 6. 变更清单（按项目）

### project1_fdic_spatial

| 文件 | 变更 |
| --- | --- |
| `README.md` | 新增「立项六问」整节；关键口径 1/2/3/8 改写（BRNUM 52.19%、无 CBSA_METRO、分年代精度、H3 实测）；已知限制 5 改写 + 新增 8（缺失非随机） |
| `data_raw/README.md` | 来源表加「核实方式」列（官方链接 + 6/30 基准日 + 下半年发布）；新增「可信度评估」整节 |
| `config/schema.yaml` | 13 个字段全部补 `note`（判定依据）；修正 `SIMS_PROJECTION` 与 `MSABR` 的错误描述 |
| `config/assertions.yaml` | `coordinate_exact_ratio` 改为分年代 + `EXACT ∪ US_Rooftop` |
| `02_profile/02_profile.py` | `fields.csv` 增 `description`/`level_reason`/`n_issues`；`profile.md` 增「判定依据」节；修正 CBSA_METRO 注释 |
| `04_validate/04_validate.py` + `README.md` | 坐标精度断言改为分年代求值 |
| `05_map/README.md` | H3 R8 实测值 + `grid_disk(k=23)` 是候选窗口而非 10 km 环 |
| `06_estimate/06_estimate.py` + `output/estimate.json` | 坐标精度口径改为分年代（保留 `supersedes` 标记） |
| `08_conclude/*` | 已知限制改写 + 新增「缺失非随机」 |
| `logs/README.md` | 清理过期迁移说明，说明 `*.log` 被 gitignore |

### project2_fdic_survival

| 文件 | 变更 |
| --- | --- |
| `config/mapping.yaml` | H3 res 8 → **7**（与实现一致），附实测尺度与理由 |
| `05_map/05_map.py` | 修正两处写死的边长数值 |
| `config/schema.yaml` | 10 个字段全部补 `note`（判定依据） |
| `02_profile/02_profile.py` | `fields.csv` 增三列 + `profile.md` 增「判定依据」节 |
| `data_raw/README.md` | 来源表加核实方式（修正「6 月前后发布」）；BRNUM 50.11% → 52.19%；新增「可信度评估」整节 |

### project6_spatial_teaching

| 文件 | 变更 |
| --- | --- |
| `data_raw/README.md` | 重写：SNAP 官方引用、年代更正（2008–2010 / 2009–2010）、`sz_bike` 真实命名与实测规模、Brightkite 行数差异、可信度评估 |
| `README.md` | 许可表更正（去掉错误的 2010–2013）、补 SNAP 引用、`logs/` 与 `output_legacy_v1/` 说明、合成参数补实测值 |
| `config/schema.yaml` | 3 个公共字段补 `note`（判定依据）；`per_dataset` 的年代 / 命名 / 行数差异说明更正 |
| `02_profile/02_profile.py` | `fields.csv` 增 `level_reason` / `n_issues`；`profile.md` 增「判定依据」节 |
| `04_validate/04_validate.py` | 未实现的 `custom` 断言不再静默通过 |
| `config/assertions.yaml` | 说明 `custom` 的求值位置（04 vs 05） |
| `output_legacy_v1/README.md` | **新增**：旧→新阶段映射 + 为什么不能直接比数字 |
| `logs/README.md` | 清理过期迁移说明，说明 `*.log` 被 gitignore |

### datakit（共享 SDK）

| 文件 | 变更 |
| --- | --- |
| `datakit/clean.py` | `on_violation=to_null` 的决策详情加入机器可读标记，使「越界置空」与「截断」分开记账（规范 §2-③） |

---

## 7. 重跑记录

| 项目 | 阶段 | 结果 |
| --- | --- | --- |
| p1 | ② 画像 / ③ 清洗 / ④ 校验 | ✅ 13 字段、等级分布不变（`required 4 / important 5 / optional 3 / ignore 1`）；越界置空 5 条正确记账；8/8 断言通过、无 P0 阻断 |
| p2 | ② 画像 / ③ 清洗 / ④ 校验 | ✅ 10 字段、等级分布不变；越界置空 5 条正确记账；8/9 断言通过（唯一失败项 `death_after_birth` 为既有 P1，已记入已知限制） |
| p6 | ② 画像（4 个数据集全量） | ✅ 19,915,182 行、3 字段 × 4 数据集；`level_reason` 已落地；违规 7 条（fdic 3 / brightkite 3 / gowalla 1） |

p1 / p2 的 ⑤–⑧ 阶段产物未重跑（输入数据未变，结论与指标保持原值），`SUMMARY.md` 已自动重建且八阶段齐全。
p6 的 ③–⑦ 同理未重跑；② 的新增列不影响下游（下游按列名取值）。
